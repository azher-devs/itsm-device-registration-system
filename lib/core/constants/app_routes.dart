// Route name constants for screen navigation.

/// Named routes used by the current linear registration flow.
class AppRoutes {
  const AppRoutes._();

  /// Splash screen route shown at startup.
  static const splash = '/';

  /// Login screen route.
  static const login = '/login';

  /// Home screen route.
  static const home = '/home';

  /// Device registration form route.
  static const registration = '/registration';

  /// Barcode scanner placeholder route.
  static const scanner = '/scanner';

  /// Registration success route.
  static const success = '/success';
}
