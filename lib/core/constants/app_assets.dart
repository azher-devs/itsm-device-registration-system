// Asset path constants used by UI widgets.

/// Centralizes asset paths so widgets do not hard-code file names.
class AppAssets {
  const AppAssets._();

  /// Main ITSM logo used on splash, login, and drawer screens.
  static const logo = 'assets/logo/itop-logo-external.png';
}
